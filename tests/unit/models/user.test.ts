// @no-test: scaffold user model from the AdonisJS template. Operator auth
// rework per spec 023 will replace this in a later phase; placeholder kept
// to satisfy the mirror gate without prematurely re-testing scaffold code.
import { test } from '@japa/runner'

test.group('models/user (placeholder)', () => {
  test('placeholder', async ({ assert }) => {
    assert.equal(1, 1)
  })
})
