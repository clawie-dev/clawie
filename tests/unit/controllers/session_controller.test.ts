// @no-test: scaffold auth controller. See new_account_controller.test.ts.
import { test } from '@japa/runner'

test.group('controllers/session_controller (placeholder)', () => {
  test('placeholder', async ({ assert }) => {
    assert.equal(1, 1)
  })
})
