// @no-test: scaffold auth controller from the AdonisJS template, retained for
// Phase 1 but not the framework focus. Will gain real tests in a later phase
// when operator auth is reworked per spec 023.
import { test } from '@japa/runner'

test.group('controllers/new_account_controller (placeholder)', () => {
  test('placeholder', async ({ assert }) => {
    assert.equal(1, 1)
  })
})
